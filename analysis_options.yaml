include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    deprecated_member_use: ignore
    dead_code: ignore
    use_build_context_synchronously: ignore # Ye line add karein

linter:
  rules:
    avoid_print: false
    file_names: false
